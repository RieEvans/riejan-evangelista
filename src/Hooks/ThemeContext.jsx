import React, { createContext } from 'react'

export const ToggleContext = createContext()

export const ThemeContext = () => {
  return (
    Throw ('Application must be Inside the Provider') 
  )
}
